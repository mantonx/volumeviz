/**
 * Main application layout component for VolumeViz.
 *
 * Provides the core shell structure including responsive sidebar navigation,
 * header with breadcrumbs, main content area, and automatic theme management.
 *
 * Used as the root layout wrapper for all authenticated pages.
 */
export { Layout } from './Layout';
export type { LayoutProps } from './Layout.types';
