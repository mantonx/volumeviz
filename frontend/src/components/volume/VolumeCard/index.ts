/**
 * VolumeCard component for displaying Docker volume information.
 *
 * Primary volume display component used throughout VolumeViz for:
 * - Volume dashboard grid layouts
 * - Search result displays
 * - Embedded volume references
 * - Volume management interfaces
 *
 * Features real-time size information, status indicators, quick actions,
 * and responsive design with multiple variants for different use cases.
 */
export { VolumeCard } from './VolumeCard';
export type {
  VolumeCardProps,
  VolumeData,
  VolumeCardVariant,
} from './VolumeCard.types';
