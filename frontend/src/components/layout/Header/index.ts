/**
 * Application header component for VolumeViz.
 *
 * Provides navigation controls, system status indicators, and user actions.
 * Features mobile sidebar toggle, API status monitoring, theme switching,
 * and access to settings and user menu.
 */
export { Header } from './Header';
export type { HeaderProps, ThemeOption, ApiStatus } from './Header.types';
