/**
 * Badge component for VolumeViz status indicators and labels.
 *
 * Provides semantic color coding for different application states:
 * - Volume status (active/inactive)
 * - Container health (healthy/unhealthy)
 * - Scan operations (scanning/complete/failed)
 * - Docker drivers and API connectivity
 */
export { Badge } from './Badge';
export type { BadgeProps, BadgeVariant } from './Badge.types';
